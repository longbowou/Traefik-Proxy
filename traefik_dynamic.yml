################################################################
# Http
################################################################

http:
  middlewares:
    traefik-dashboard-auth:
      basicAuth:
        users:
          - "admin:1RAxezGFfvn@"

  routers:
    traefik-dashboard:
      rule: "Host(`traefik.longbowou.com`)"
        - traefik-dashboard-auth
      entrypoints:
        - http
        - https
      service: "api@internal"
      tls:
        certResolver: le
